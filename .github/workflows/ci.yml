name: ci

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go test ./...

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: go test s5 e2e failure matrix
        run: go test -tags=e2e ./internal/commands -run TestS5E2EWorktreePRSyncMergeFailureMatrix -count=1
      - name: Check for e2e token
        id: check-token
        run: |
          if [ -n "${{ secrets.AGENCY_GH_TOKEN }}" ]; then
            echo "has_token=true" >> $GITHUB_OUTPUT
          else
            echo "has_token=false" >> $GITHUB_OUTPUT
            echo "::notice::Skipping GH-backed S5 happy-path e2e - AGENCY_GH_TOKEN not configured"
          fi
      - name: gh auth status
        if: steps.check-token.outputs.has_token == 'true'
        env:
          GH_TOKEN: ${{ secrets.AGENCY_GH_TOKEN }}
        run: gh auth status
      - name: go test s5 gh happy path e2e
        if: steps.check-token.outputs.has_token == 'true'
        env:
          GH_TOKEN: ${{ secrets.AGENCY_GH_TOKEN }}
          AGENCY_GH_E2E: "1"
          AGENCY_GH_REPO: "NielsdaWheelz/agency-test"
        run: go test -tags=e2e ./internal/commands -run TestGHE2EWorktreePRSyncMerge -count=1
