# S1 PR-02 Gate Item Lifecycle + Closure Policy - Decisions

Last updated: 2026-02-17
Status: draft

## decision ledger

| ID | Problem | Decision | Rejected alternatives | Invariant impact | Test impact | Fallback/Default | Owner | Due |
|---|---|---|---|---|---|---|---|---|
| PR02-D-001 | How should Gate A vs Gate B be resolved for role policy checks? | Resolve membership via canonical `release-gates.md` parsing (PR-01 source parser), not by priority labels alone. | Priority-only classification; hardcoded per-issue allowlists in code. | Preserves L2 invariant that gate sets are exactly release-gates sections A/B. | Add tests where gate membership controls approval behavior. | Unresolvable membership => `E_GATE_TRANSITION_INVALID`. | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-002 | Should transition requests require `from_state` to match current parsed issue state? | Yes, require exact match and reject mismatch. | Ignoring `from_state`; best-effort transition from current state. | Keeps lifecycle transitions deterministic and auditable. | Add mismatch test asserting `E_GATE_TRANSITION_INVALID`. | none | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-003 | How should transition `reason` be interpreted when L2 has both model-level reason field and reopen-specific non-empty requirement? | Keep `reason` present in transition shape for all transitions; require non-empty only for reopen (`closed -> in_progress`). | Requiring non-empty reason for every transition; dropping reason from non-reopen requests. | Preserves reopen-auditability invariant without over-constraining non-reopen flow. | Add `TestTransition_NonReopenAllowsEmptyReason` and `TestTransition_ReopenRequiresReasonAndEvidence`. | Empty reason outside reopen is accepted. | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-004 | How should closure precondition failures be surfaced in transition flow? | Reuse PR-01 evaluator precondition output and return its `blocking_code`. | Duplicating precondition logic in transition engine; generic invalid-transition errors. | Preserves shared item-level error semantics across evaluate and transition surfaces. | Add closure-guard tests asserting item-level error codes. | If no blocking code is present, use guard-specific PR-02 error code. | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-005 | How should role failures map to errors? | Policy role mismatch for closure maps to `E_GATE_APPROVAL_REQUIRED`; malformed role values map to `E_GATE_TRANSITION_INVALID`. | Mapping all role issues to one code; introducing new role-format error code. | Keeps policy-vs-shape failures distinct while preserving current error model. | Add `TestTransition_P0CloseRequiresMaintainer`, `TestTransition_GateBCloseByContributorRejected`, and `TestTransition_InvalidActorRoleReturnsTransitionInvalid`. | none | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-006 | What is required for reopen transitions? | `closed -> in_progress` requires non-empty `reason` and non-empty `evidence_refs`; missing either returns `E_GATE_REOPEN_REASON_REQUIRED`. | Reason-only requirement; evidence-only requirement; generic invalid-transition errors. | Preserves reopen auditability invariant. | Add reopen tests for missing reason/evidence/both. | none | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-007 | How should GH e2e closure policy be enforced? | For `requires_gh_e2e=true`, closure requires at least one passing evidence row with `scope=e2e_opt_in`; otherwise `E_GATE_E2E_REQUIRED`. | Inferring e2e from command strings only; allowing non-e2e evidence for flagged items. | Preserves GH mutation safety invariant. | Add closure test with missing e2e scope evidence. | none | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-008 | How should design-item enforcement be evaluated without runtime-diff detection? | Require at least one passing evidence row in enforcement scopes (`targeted`, `suite`, `e2e_opt_in`, `lint`, `format`, `vet`). | Requiring runtime-diff detection in PR-02; skipping design enforcement entirely. | Preserves design-item closure invariant with deterministic available inputs. | Add design closure failure/success tests. | Evidence-based enforcement is default until diff-aware capability exists. | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-009 | How is PR-01 open default about evaluate transport behavior resolved before PR-02 implementation? | Canonical behavior is library-level: return typed results + `AgencyError` codes; transport adapter mapping is deferred to adapter-owning PR. | Blocking PR-02 on daemon route selection; forcing daemon routing in PR-02. | Keeps PR boundaries intact while providing deterministic behavior semantics. | PR-02 tests assert library behavior directly. | Adapter mapping deferred, but must remain 1:1 with library errors. | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-010 | Should successful transitions mutate issue state or only validate? | Apply successful transitions by persisting `state:` in issue stubs. | Validator-only transitions with no persisted state. | Enables lifecycle state progression to be observable in subsequent evaluations. | Add `TestTransition_PersistsStateByReplacingExistingStateLine` and `TestTransition_PersistsStateByInsertingAfterLabelsWhenStateMissing`. | write failure => `E_GATE_TRANSITION_INVALID` with details. | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-011 | Should parser accept unknown evidence scope/result values? | No; unknown `scope` or `result` is `E_GATE_ITEM_INVALID`. | Permissive parsing with downstream guard checks; silent normalization of unknown values. | Preserves deterministic closure-policy enforcement inputs. | Add parser tests for invalid scope/result rejection. | none | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-012 | How should transition logic choose one error when multiple transition checks fail? | Define strict transition-validation and closure-guard precedence orders and enforce them in tests. | First-failure by incidental code path; unordered/map-iteration-driven selection. | Preserves deterministic error semantics for clients and reporting flows. | Add `TestTransition_ErrorPrecedenceIsDeterministic`. | Any precedence change requires explicit spec + test updates in same PR. | `@nnandal` + `Codex` | fixed in PR-02 |
| PR02-D-013 | Should transition-critical issue metadata be permissive or strict-invalid? | Treat missing/invalid `priority`, missing/invalid `type`, and invalid explicit `state:` as `E_GATE_ITEM_INVALID`; keep default `state=open` only when state metadata is absent. | Silent normalization to defaults (for example invalid state -> open); deferring metadata validation to later PRs. | Ensures GH e2e/design closure guards run on trustworthy metadata inputs. | Add parser tests for invalid explicit state, missing priority, and missing type. | `state=open` default applies only when no `state:` metadata line exists. | `@nnandal` + `Codex` | fixed in PR-02 |

## open decisions

| ID | Question | Temporary default | Owner | Due |
|---|---|---|---|---|
| none | n/a | n/a | n/a | n/a |
