# S1 PR-05 Release Gate Enforcement + Closure Reporting - Decisions

Last updated: 2026-02-18
Status: draft

## decision ledger

| ID | Problem | Decision | Rejected alternatives | Invariant impact | Test impact | Fallback/Default | Owner | Due |
|---|---|---|---|---|---|---|---|---|
| PR05-D-001 | Where should PR-05 release-readiness enforcement be operationalized? | Daemon-first read endpoints with daemonclient + command consumers. | Local-only CLI file parsing; CI-only library invocation with no daemon surface. | Aligns release-governance consumption with daemon authority and avoids split-brain readiness decisions. | Add daemon handler/client/command tests for ready + blocked paths. | If daemon is unavailable, PR-05 does not define a local fallback path. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-002 | How should blocked readiness be determined without ad-hoc interpretation? | Reuse PR-03 `RequireSliceReady` directly and propagate `E_GATE_BLOCKED` details unchanged. | Recomputing gate readiness in PR-05; custom blocked heuristics in command layer. | Preserves invariant continuity between aggregate readiness and release gating. | Add tests asserting detail-key parity with PR-03 blocked contract. | Any readiness semantic change must be made upstream (PR-03 contract) first. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-003 | How should closure evidence reporting remain deterministic and auditable? | Report is built from canonical gate order + parsed closure evidence for closed+valid items only, with fixed schema keys. | Free-form markdown report output; unordered map-based JSON payloads; including invalid-closed items as closure evidence. | Preserves closure-evidence invariant while preventing report churn and ambiguity. | Add ordering + schema-stability tests and mixed-state fixture tests. | Report fails deterministically on malformed required evidence rather than silently skipping. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-004 | How should freeze-readiness unresolved-default semantics be enforced? | Parse Section 9 table in `docs/v2.1/s1/s1_spec.md`; any unresolved data row blocks freeze. | Manual governance checks outside code path; separate sidecar defaults file not tied to spec. | Directly enforces invariant 11 and acceptance scenario for freeze blocking. | Add tests for unresolved rows, empty table success, and malformed spec handling. | If spec parsing fails, return deterministic source-invalid contract error. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-005 | Should PR-05 add new error code families for freeze blocking/reporting failures? | No new error family; reuse `E_GATE_BLOCKED` and existing S1 gate errors with fixed details. | Introducing new `E_GATE_FREEZE_BLOCKED`/`E_GATE_REPORT_*` codes mid-slice. | Keeps S1 error model stable and avoids contract widening late in slice. | Add tests asserting exact existing code usage and detail schema determinism. | Any new error family requires upstream L2 error-model update first. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-006 | How should PR-05 handle `internal/s1gates` temporary namespace cruft? | Hard-delete `internal/s1gates` in PR-05 after full parity migration to `internal/releasegates`. | Deferring deletion to a later cleanup PR; keeping dual namespaces active. | Removes temporary slice-scoped namespace debt before S2 and avoids future consumer drift. | Add migration parity tests and repository-level no-legacy-reference verification. | No fallback: PR-05 is incomplete until `internal/s1gates` is removed. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-007 | What should be the deterministic evaluation order for combined release governance checks? | Run release-readiness first, then closure report/freeze checks; blocked readiness can short-circuit readiness command while report/freeze commands remain independently callable. | Unordered parallel checks with nondeterministic first failure; always-running all checks for readiness path. | Stabilizes automation behavior and keeps readiness command contract simple and predictable. | Add tests for readiness short-circuit and independent report/freeze endpoint execution. | If future product needs combined multi-status response, add explicit new endpoint contract. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-008 | Which command surface should expose PR-05 operational checks? | Add `agency repo s1` command family backed by daemon endpoints. | Embedding checks only in `merge`/`push`; no CLI entrypoint (library-only). | Provides explicit, scriptable release-governance surface without mutating existing merge/push flows. | Add command tests for human and `--json` outputs with blocked/success cases. | Existing merge/push behavior remains unchanged in PR-05 unless explicitly revised later. | `@nnandal` + `Codex` | fixed in PR-05 |
| PR05-D-009 | What is the long-term issue source model after S1 markdown issue stubs? | GitHub issues become canonical after S1; PR-05 introduces source abstraction and treats markdown issue stubs as compatibility-only provider. | Continuing direct markdown-only issue parsing in service/daemon consumers; attempting full GH provider delivery inside PR-05 scope. | Preserves S1 contract behavior while creating enterprise migration path away from local markdown issue artifacts. | Add adapter contract tests and ensure service logic uses source interface instead of direct markdown coupling. | Until GH provider lands, markdown adapter remains enabled behind the interface. | `@nnandal` + `Codex` | fixed in PR-05 |

## open decisions

| ID | Question | Temporary default | Owner | Due |
|---|---|---|---|---|
| none | n/a | n/a | n/a | n/a |
