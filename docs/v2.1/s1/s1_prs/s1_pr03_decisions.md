# S1 PR-03 Gate + Slice Readiness Evaluation - Decisions

Last updated: 2026-02-17
Status: draft

## decision ledger

| ID | Problem | Decision | Rejected alternatives | Invariant impact | Test impact | Fallback/Default | Owner | Due |
|---|---|---|---|---|---|---|---|---|
| PR03-D-001 | How should PR-03 define drift when `issue-map.md` includes broader cross-slice inventory than Gate A/B? | Treat `release-gates.md` Gate A/B as canonical and declare drift only when any gate issue is missing from `issue-map.md` or appears more than once there. | Full-document equality (`release-gates` == `issue-map`); ignoring issue-map entirely. | Preserves canonical gate membership invariant while enforcing synchronization invariant without false-positive drift. | Add missing and duplicate drift tests that assert `E_GATE_SET_DRIFT`. | Non-gate issue-map entries are allowed and ignored by drift check. | `@nnandal` + `Codex` | fixed in PR-03 |
| PR03-D-002 | L2 defines `E_GATE_BLOCKED` but `/gates/evaluate` request shape has no "strict readiness required" field; how should blocked semantics be exposed? | Keep `EvaluateGates` as pure aggregate evaluation returning `slice_ready` true/false, and add `RequireSliceReady` helper that returns `E_GATE_BLOCKED` when `slice_ready=false`. | Making blocked state an error in `EvaluateGates`; inventing request flags not present in L2 request contract. | Preserves evaluate response semantics and gives deterministic blocked-enforcement primitive for release gating flows. | Add helper tests for blocked and ready paths, including blocker details assertions. | Downstream release-facing callers must invoke `RequireSliceReady`. | `@nnandal` + `Codex` | fixed in PR-03 |
| PR03-D-003 | Should aggregate readiness treat any `state=closed` item as closed, or require full closure semantics? | Count closed only when `state=closed` and evaluator reports no blocking requirements (`blocking_code` empty). | State-only counting; counting based solely on acceptance checkbox status. | Aligns aggregate readiness with closed-item invariant requiring acceptance/tests/evidence completeness. | Add test where `state=closed` with missing evidence remains blocking. | If evaluator contracts change, aggregate closed predicate must be updated and re-tested in same PR. | `@nnandal` + `Codex` | fixed in PR-03 |
| PR03-D-004 | How should aggregate evaluation surface per-item artifact failures while honoring aggregate API error contract? | Map per-item `E_GATE_ITEM_NOT_FOUND`/`E_GATE_ITEM_INVALID` failures to aggregate `E_GATE_SET_INVALID`, carrying `issue_path` and `item_error_code` in details. | Propagating item-level errors directly from aggregate endpoint; collapsing to generic internal error without cause details. | Keeps aggregate error model stable and operator-actionable while preserving root cause traceability. | Add item-artifact failure test asserting top-level `E_GATE_SET_INVALID` plus item-cause details. | Unknown item error codes default to `E_GATE_SET_INVALID` with raw error message details. | `@nnandal` + `Codex` | fixed in PR-03 |
| PR03-D-005 | How should `blocking_items` be ordered to avoid nondeterministic reporting churn? | Preserve canonical ordering from Gate A/B sections in `release-gates.md`. | Alphabetical ordering; map-iteration order; issue-map ordering. | Stabilizes aggregate output contract and keeps blocker output aligned to source-of-truth gate ordering. | Add explicit ordering test against canonical gate-source sequence. | none | `@nnandal` + `Codex` | fixed in PR-03 |
| PR03-D-006 | How should `EvaluateGates` choose one error when multiple aggregate failures are present? | Enforce deterministic precedence: gate source invalid -> issue-map invalid -> item artifact invalid -> drift -> success. | Incidental first-failure behavior by code path order; unordered parallel checks. | Stabilizes aggregate error semantics for clients, CI, and release automation. | Add precedence test with fixtures that trigger overlapping failure classes. | Any precedence change requires explicit spec + test update in same PR. | `@nnandal` + `Codex` | fixed in PR-03 |
| PR03-D-007 | How should aggregate errors encode details so tooling is stable across releases? | Define fixed details key schema for `E_GATE_SET_INVALID` (item-artifact case), `E_GATE_SET_DRIFT`, and `E_GATE_BLOCKED`; encode blocker arrays as canonical-order `|`-joined strings. | Free-form per-call details; JSON blobs under one key; implicit/undocumented key naming. | Makes error payloads deterministic and contract-safe for CLI/report/CI consumers. | Add tests asserting key presence and deterministic encoded values for drift/item-failure/blocked errors. | Any key-set or encoding change must be reflected in spec + tests in same PR. | `@nnandal` + `Codex` | fixed in PR-03 |
| PR03-D-008 | How should drift details be selected when multiple gate issues mismatch issue-map counts? | Report the first mismatching gate issue path in canonical gate order with its observed count and mismatch kind. | Reporting an arbitrary mismatch from map iteration; reporting all mismatches in one unstructured message. | Preserves deterministic single-cause error reporting while keeping output contract small and stable. | Add drift-details ordering test with multiple mismatches. | none | `@nnandal` + `Codex` | fixed in PR-03 |

## open decisions

| ID | Question | Temporary default | Owner | Due |
|---|---|---|---|---|
| none | n/a | n/a | n/a | n/a |
