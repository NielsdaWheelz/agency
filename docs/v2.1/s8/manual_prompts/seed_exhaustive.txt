Work only inside this repository.
Prefer built-in Read/Grep/Glob/Edit/Write tools when available; otherwise use equivalent repo-safe shell commands.
Do not modify any tracked files. Only create or update files under `tmp/s8-manual/`.

1. Read `README.md`, `docs/getting-started.md`, `docs/cli.md`, `internal/daemon/read_timeline.go`, and `internal/daemon/stream/families.go`.
2. Search the repo for `agency.followup_prompt`, `checkpoint ls`, `ActionFamilyWebAction`, and `tool_call`.
3. If you have a glob tool, list the `*.go` files directly under `internal/daemon/stream/`; otherwise use an equivalent repo-safe command.
4. Create `tmp/s8-manual/notes.md` with:
   - a title
   - four short bullet points summarizing what you found
   - a final line exactly `seed-line: one`
5. Create `tmp/s8-manual/search.txt` with a short list of the search hits or file paths you used.
6. Run exactly:
   `sh -lc 'printf "cmd-start\n"; i=1; while [ "$i" -le 6 ]; do printf "row-%02d\n" "$i"; i=$((i+1)); done; sleep 20; printf "cmd-end\n"'`
7. Summarize what you read, what you searched, what you changed under `tmp/s8-manual/`, and the command output.
