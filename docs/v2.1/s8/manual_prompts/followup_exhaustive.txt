Only modify files under `tmp/s8-manual/`.

Re-open `tmp/s8-manual/notes.md` and `tmp/s8-manual/search.txt`.
Append exactly `followup-line: two` to `tmp/s8-manual/notes.md`.

Prefer a native grep/search tool if available; otherwise use `rg`.
Search `README.md`, `docs/cli.md`, and `internal/` for `agency.followup_prompt`, `checkpoint ls`, `agency watch`, and `tool_call`.

If you have a glob tool, also list files matching `*history*.go` under `internal/`.

Append a `history-check` section to `tmp/s8-manual/search.txt` with the best matching paths or hits.
Finally, summarize both tracked lines now present in `tmp/s8-manual/notes.md` and what you added to `tmp/s8-manual/search.txt`.
