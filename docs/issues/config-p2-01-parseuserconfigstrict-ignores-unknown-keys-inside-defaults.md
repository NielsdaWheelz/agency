# [p2][config][tech-debt] parseUserConfigStrict ignores unknown keys inside defaults

labels: `p2`, `type:tech-debt`, `area:config`

## summary
parseUserConfigStrict ignores unknown keys inside defaults

## context
- section: Audit: Config
- source: docs/issues.md
- details:
  - typos are silently accepted; treat as invalid config.

## acceptance criteria
- [ ] define minimal fix + tests

