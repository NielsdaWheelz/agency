# [p1][landing][tech-debt] repo lock is assumed, not enforced

labels: `p1`, `type:tech-debt`, `area:landing`

## summary
repo lock is assumed, not enforced

## context
- section: Audit: Landing
- source: docs/issues.md
- details:
  - service comments say “under repo lock,” but it doesn’t lock internally.

## acceptance criteria
- [ ] define minimal fix + tests

