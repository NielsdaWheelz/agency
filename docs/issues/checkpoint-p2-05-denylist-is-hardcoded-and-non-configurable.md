# [p2][checkpoint][tech-debt] denylist is hardcoded and non-configurable

labels: `p2`, `type:tech-debt`, `area:checkpoint`

## summary
denylist is hardcoded and non-configurable

## context
- section: Audit: Checkpoint
- source: docs/issues.md
- details:
  - no way to tune for enterprise policies; also only matches basenames.

## acceptance criteria
- [ ] define minimal fix + tests

